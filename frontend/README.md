
# Frontend (Vue 3 + Vite)

## Prereqs
- Node.js v20+

## Setup
```bash
cd frontend
npm install
cp .env.example .env
# Ensure VITE_API_BASE_URL points to your FastAPI server (default http://localhost:8000)
npm run dev
```
